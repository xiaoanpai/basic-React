表现层组件就是指渲染UI元素的组件。
它们并没有和任何数据架构紧密地耦合在一起。
它们通过属性接收数据，并且通过回调函数属性将数据回传给它们的父组件。

容器组件就是将表现层组件和数据相连的组件。
容器组件将会通过上下文访问Store,并且使用Store管理任意交互。
它们通过映射属性到State和将回调函数属性传递给Store的dispatch方法，来达到渲染表现层组件的目的。
容器组件关注的重点并不在UI元素，它们只是用来将表现层组件连接到数据。