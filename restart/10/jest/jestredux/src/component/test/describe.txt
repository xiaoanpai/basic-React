Enzyme为测试程序处理渲染React组件的任务，并为遍历子元素、验证属性、验证State、模拟事件和DOM查询提供了必要的工具

shoallow  为单元测试在一定层级深度上渲染组件。

mount 会使用浏览器DOM渲染组件， 当用户需要测试完整的组件生命周期,以及子元素的属性或者State时，这是必需的。

render 用于渲染包含静态HTML标记的组件。通过render,用户可以对组件验证是否能够返回相应的HTML.